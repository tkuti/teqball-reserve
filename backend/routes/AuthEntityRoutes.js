const AuthEntityController = require("../controllers/AuthEntityController");
const express = require("express");
const router = express.Router();


//router.get("/", AuthEntityController.getUsers);


module.exports = router;